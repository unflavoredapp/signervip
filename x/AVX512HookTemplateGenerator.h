//
//  AVX512HookTemplateGenerator.h
//  AVX512 by DELvEK.NET
//

#import <UIKit/UIKit.h>

/// Multi-select class picker that generates a starter swizzle-dylib source file
/// for the selected classes. Recon -> pick -> generate -> edit -> build.
@interface AVX512HookTemplateGenerator : UITableViewController
@end
